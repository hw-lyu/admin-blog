name: My Workflow test

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Use Secret in Workflow
      run: echo "My secret value is ${{ secrets.TEST }}"
